{% import "renobase.html.twig" as m %}
<table class="ui striped very compact table">
    <thead>
        <tr>
            <th>#</th>
            <th>Nodes</th>
            <th>File</th>
            <th>Instruction</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        {% for row in data %}
            <tr>
                <td class="top aligned collapsing">
                    <a name="{{row.runitem.id}}" class="anchor">&nbsp;</a>{{loop.index}}
                </td>
                <td class="top aligned">
                    {{ m.prettyDisplay(row.params.Nodes) }}
                </td>
                <td class="top aligned">
                    {{ m.prettyDisplay(row.params.File)}}
                </td>
                <td class="top aligned" style="max-width: 480px; overflow-x: auto;">
                    {{ m.prettyDisplay(row.params.Instruction) }}
                </td>
                <td class="top aligned collapsing">
                    {{ m.runItemStatusControl(row.runitem) }}
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

